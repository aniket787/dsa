1). reverse array in place //
2).Sort binary array //
3). Move zeroes //
4).Rotate array left/right // 
5).First and Last position of an element in sorted array //
6). Container With Most Water //
7). Intersection of two arrays
8). There is an array with every element repeated twice except one. Find that element
9). Find Kth smallest / Largest in unsorted array
10). Find common elements in three sorted arrays
11). Find first repeating elem in array
12). Find first non - repeating elem in array
13). How to find top two numbers from an integer array
14). How to find if there is a sub array with sum equal to zero?
15). Merge sort in array with and without extra space
16). How to find sub array with maximum sum in an array of positive and negative number
17). Write a program to find length of longest consecutive sequence in array of integers
18). Given an array of of size n and a number k, find all elements that appear more than n/k times?
19). How to find the missing number in integer array of 1 to 100?
20). Find/remove duplicate number
21). Sort an array in wave form 
22). Kth missing element in array
23). Majority element 
24). Leaders in array
25). Find pairs whose sum is equal to given target  
26). Find repeated numbers  
27). Two sum
28). three sum 
29). Lexicographical order
30). Largest and small element in unsorted array
31). kadanes algo
32). Search insert position
33). Maximum Subarray
34). Count primes
35). Find first/second/third largest element
36). Sort array using QuickSort
